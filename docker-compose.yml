services:
  kudondzaui:
    build: .    
    image: kudondza-ui:v1
    ports:
      - 4205:80
    expose:
      - 80
    networks:
      - kudondza-ui-net

networks:
  kudondza-ui-net:
    driver: bridge


# Para levantar usando o docker-compose
# docker-compose up -d --build

# Usando o DockerFile apenas(Sem usar o docker-compose) :
# docker build -t sci-kudondza-ui:v1 .
# docker run -p 4200:80 sci-kudondza-ui:v1
# docker run -p 4200:80 -d kudondza-ui:v1    #Liberar o terminal

# Para destruir e remover tudo
# docker compose down
# docker compose down --volumes

